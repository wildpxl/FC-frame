import { AxiosError } from "axios";
import { ErrorRes } from "../v1";
/**
 * Utility for parsing errors returned by the Neynar API servers. Returns true
 * if the given error is caused by an error response from the server, and
 * narrows the type of `error` accordingly.
 */
export declare const isApiErrorResponse: (error: any) => error is import("type-fest/source/simplify").Simplify<import("type-fest").Except<AxiosError<ErrorRes, any>, "response"> & Required<Pick<AxiosError<ErrorRes, any>, "response">>>;
